import { DoCalcDirective } from './doCalc.directive';

describe('DoCalcDirective', () => {
  it('should create an instance', () => {
    //const directive = new TablekeyDirective();
    //expect(directive).toBeTruthy();
  });
});
