import { TablekeyDirective } from './tablekey.directive';

describe('TablekeyDirective', () => {
  it('should create an instance', () => {
    //const directive = new TablekeyDirective();
    //expect(directive).toBeTruthy();
  });
});
